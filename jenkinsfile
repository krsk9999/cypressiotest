pipeline {
    agent any

    tools {nodejs "Node 13.1.0"}

    stages {
        stage('Dependencies') {
            steps {
                bat 'npm i'
            }
        }
        stage('e2e Tests') {
            steps {
                bat 'npm run cy:debug'
            }
        }

    }
}
