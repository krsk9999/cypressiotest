/*
describe("App testing First Module", () => {
	
    it('Navigate to Google', () => {
        cy.visit('https://google.com')
        .viewport('macbook-15')
        .get('input[name="q"]')
        .type('EStamos buscando ver como insertar algo en google para hacer una busqueda bien grandota...')
        .type('{enter}');
        
    });


	it("should load example webpage", () => {
		cy.visit(Cypress.env('baseUrl'))
			.url()
			.should("include", "example.com")
			.title()
			.should("eq", "Example Domain")
			.get("h1")
			.contains("Example Domain")

		cy.wait(3000)
		// cy.pause() //this doesnt work on unattended tests

		cy.get("h1").as("title")
		cy.get("@title").contains("Example Domain")
		cy.get("@title").should("be.visible")
    })

    
    beforeEach(()=>{
        cy.visit
    })

})
*/
